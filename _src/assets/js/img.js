'use strict';

const button = document.querySelector('.button__thumbnail');
const photo = document.querySelector('.photo');
const thumbnail = document.querySelector('.thumbnail__image');

function photoChange(event) {
    
}

button.addEventListener('click', photoChange);